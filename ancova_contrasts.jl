using Distributions
# cdf() is equivalent to p[dist] in R
# quantile() is equivalent to q[dist] in R

# No interaction effect

Unadjusted_SD = 24.7

SD_04 = sqrt(24.7^2*(1-0.4^2))
SD_045 = sqrt(24.7^2*(1-0.45^2))
SD_05 = sqrt(24.7^2*(1-0.5^2)
